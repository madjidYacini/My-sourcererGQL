#RUN project.

    •clone project
    • npm install
    • create a .env file and add : `
    REACT_APP_GITHUB_TOKEN= <YOUR GITHUB TOKEN>
    `
    • in command line, type: `npm start`

#Madjid Yacini.
